# StudyHelper 高中生助学平台 - 系统架构设计

## 文档信息

| 项目名称 | StudyHelper 高中生助学平台 |
|---------|------------------------|
| 版本号   | V1.0                   |
| 创建日期 | 2026-05-11             |
| 文档状态 | 草稿                   |

---

## 1. 系统架构图（文字版）

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户层 (Clients)                              │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                 │
│  │  PC浏览器    │    │  移动端H5   │    │   微信小程序 │                 │
│  │ Chrome/Firefox│  │   Safari    │    │  (预留)      │                 │
│  └─────────────┘    └─────────────┘    └─────────────┘                 │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         负载均衡层 (Load Balancer)                       │
│                        Nginx (反向代理 + 负载均衡)                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           应用服务层 (Application)                        │
├───────────────┬───────────────┬───────────────┬───────────────────────┤
│   网关服务     │   业务服务      │   任务服务     │    AI服务             │
│   Spring      │   知识库服务    │   学习计划     │    智能组卷算法        │
│   Gateway     │   用户服务      │   试卷生成     │    薄弱点分析          │
│   (Auth)      │   考试服务      │   提醒调度     │                       │
└───────────────┴───────────────┴───────────────┴───────────────────────┘
        │               │               │               │
        └───────────────┴───────────────┴───────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        ▼                       ▼                       ▼
┌───────────────┐    ┌───────────────────┐    ┌─────────────────┐
│  Redis缓存集群 │    │    MySQL主库      │    │    OSS对象存储   │
│  (Session/    │    │    (读写分离)      │    │    (图片/文件)   │
│   数据缓存)    │    │                   │    │                 │
└───────────────┘    └───────────────────┘    └─────────────────┘
```

### 1.2 前端架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Vue3 应用                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Views     │  │ Components  │  │  Router     │             │
│  │   (页面)    │  │  (组件库)   │  │  (路由)     │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Store     │  │   API       │  │  Utils      │             │
│  │  (状态管理) │  │  (接口封装) │  │  (工具函数) │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 后端架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Spring Boot 应用                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  Controller │  │  Interceptor │  │  Filter     │             │
│  │   (控制器)  │  │   (拦截器)   │  │  (过滤器)   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│           │                                    │               │
│           └────────────────┬───────────────────┘               │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      Service 层                           │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌─────────┐  │   │
│  │  │ 用户服务   │ │ 知识库服务│ │ 学习计划  │ │ 考试服务│  │   │
│  │  │           │ │           │ │   服务    │ │         │  │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └─────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      Mapper 层                           │   │
│  │              (MyBatis-Plus 自动映射)                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐                   │
│  │   MySQL   │  │   Redis   │  │    OSS    │                   │
│  │  (持久化) │  │  (缓存)   │  │ (文件存储) │                   │
│  └───────────┘  └───────────┘  └───────────┘                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 技术选型说明

### 2.1 前端技术栈

| 技术 | 版本 | 用途 | 选型理由 |
|-----|------|------|---------|
| Vue.js | 3.4+ | 核心框架 | 渐进式框架，学习成本低，生态完善 |
| Vite | 5.0+ | 构建工具 | 极速开发启动，热更新快 |
| Element Plus | 2.5+ | UI组件库 | Vue3官方推荐，中后台组件丰富 |
| Pinia | 2.1+ | 状态管理 | Vue3官方推荐，比Vuex更轻量 |
| Vue Router | 4.0+ | 路由管理 | Vue官方路由 |
| Axios | 1.6+ | HTTP客户端 | 生态成熟，支持拦截器 |
| ECharts | 5.4+ | 图表库 | 学习数据可视化 |
| dayjs | 1.11+ | 时间处理 | 轻量级日期库 |

### 2.2 后端技术栈

| 技术 | 版本 | 用途 | 选型理由 |
|-----|------|------|---------|
| Spring Boot | 3.2+ | 核心框架 | 生态完善，社区活跃 |
| Spring Security | 6.x | 安全框架 | 认证授权一体化 |
| MyBatis-Plus | 3.5+ | ORM框架 | 增强MyBatis，减少SQL编写 |
| Redis | 7.0+ | 缓存/会话 | 高性能，支持多种数据结构 |
| JWT | 0.11+ | Token认证 | 无状态认证，适合分布式 |
| Lombok | 1.18+ | 简化代码 | 减少getter/setter |
| Hutool | 5.8+ | 工具库 | Java工具集 |
| Apache POI | 5.2+ | Office处理 | 试卷导出功能 |

### 2.3 数据库技术栈

| 技术 | 版本 | 用途 |
|-----|------|------|
| MySQL | 8.0 | 主数据库 |
| Redis | 7.0 | 缓存数据库 |
| Alibaba Druid | 1.2 | 数据库连接池 |

### 2.4 基础设施

| 技术 | 用途 |
|-----|------|
| Nginx | 反向代理、负载均衡、静态资源服务 |
| Docker | 容器化部署 |
| Jenkins/GitLab CI | 持续集成/部署 |

---

## 3. 前端架构

### 3.1 目录结构

```
src/
├── api/                      # 接口层
│   ├── user.js              # 用户相关接口
│   ├── knowledge.js         # 知识库相关接口
│   ├── plan.js              # 学习计划相关接口
│   ├── exam.js              # 考试相关接口
│   └── index.js             # 接口统一导出
│
├── assets/                   # 静态资源
│   ├── images/              # 图片资源
│   ├── icons/               # 图标资源
│   └── styles/              # 全局样式
│       ├── variables.scss   # 主题变量
│       └── global.scss      # 全局样式
│
├── components/               # 公共组件
│   ├── common/             # 通用组件
│   │   ├── PageHeader.vue  # 页面头部
│   │   ├── SearchBar.vue   # 搜索栏
│   │   └── Pagination.vue  # 分页组件
│   │
│   ├── knowledge/          # 知识库组件
│   │   ├── KnowledgeCard.vue
│   │   ├── ChapterTree.vue
│   │   └── KnowledgeDetail.vue
│   │
│   ├── plan/               # 学习计划组件
│   │   ├── PlanCard.vue
│   │   ├── TaskItem.vue
│   │   └── ProgressRing.vue
│   │
│   └── exam/               # 考试组件
│       ├── QuestionItem.vue
│       ├── PaperPreview.vue
│       └── ScoreChart.vue
│
├── composables/             # 组合式函数
│   ├── useAuth.js          # 认证相关
│   ├── useLoading.js       # 加载状态
│   └── usePagination.js    # 分页逻辑
│
├── config/                   # 配置文件
│   ├── index.js            # 应用配置
│   └── routes.js           # 路由配置
│
├── directives/              # 自定义指令
│   ├── permission.js       # 权限指令
│   └── loading.js          # 加载指令
│
├── layout/                   # 布局组件
│   ├── DefaultLayout.vue   # 默认布局
│   ├── AdminLayout.vue     # 管理后台布局
│   └── components/
│       ├── Sidebar.vue     # 侧边栏
│       ├── Header.vue      # 头部
│       └── Footer.vue      # 底部
│
├── router/                   # 路由配置
│   ├── index.js
│   └── routes/
│       ├── index.js         # 路由汇总
│       ├── user.js          # 用户路由
│       ├── knowledge.js     # 知识库路由
│       ├── plan.js          # 学习计划路由
│       └── exam.js          # 考试路由
│
├── stores/                   # 状态管理
│   ├── user.js             # 用户状态
│   ├── knowledge.js        # 知识库状态
│   ├── plan.js             # 学习计划状态
│   └── exam.js             # 考试状态
│
├── utils/                    # 工具函数
│   ├── auth.js             # 认证工具
│   ├── request.js          # Axios封装
│   ├── storage.js          # 存储工具
│   ├── validate.js         # 表单验证
│   └── format.js           # 格式化工具
│
├── views/                    # 页面视图
│   ├── Home.vue            # 首页
│   │
│   ├── auth/               # 认证模块
│   │   ├── Login.vue
│   │   └── Register.vue
│   │
│   ├── knowledge/          # 知识库模块
│   │   ├── SubjectList.vue  # 学科列表
│   │   ├── ChapterList.vue  # 章节列表
│   │   ├── KnowledgeDetail.vue # 知识点详情
│   │   └── SearchResult.vue  # 搜索结果
│   │
│   ├── plan/               # 学习计划模块
│   │   ├── PlanList.vue     # 计划列表
│   │   ├── PlanCreate.vue   # 创建计划
│   │   ├── PlanDetail.vue   # 计划详情
│   │   └── TaskCalendar.vue # 任务日历
│   │
│   ├── exam/               # 考试模块
│   │   ├── PaperList.vue    # 试卷列表
│   │   ├── PaperGenerate.vue # 生成试卷
│   │   ├── PaperPreview.vue # 试卷预览
│   │   ├── PaperDo.vue      # 在线答题
│   │   └── PaperResult.vue  # 成绩查看
│   │
│   ├── user/               # 用户模块
│   │   ├── Profile.vue      # 个人资料
│   │   ├── StudyStats.vue   # 学习统计
│   │   └── BindStudent.vue  # 绑定学生
│   │
│   └── admin/              # 管理后台
│       ├── Dashboard.vue   # 管理仪表盘
│       ├── UserManage.vue  # 用户管理
│       ├── KnowledgeManage.vue # 知识库管理
│       └── QuestionManage.vue # 题目管理
│
├── App.vue                  # 根组件
├── main.js                 # 入口文件
└── permission.js           # 路由权限控制
```

### 3.2 组件设计

#### 3.2.1 组件分层

```
┌────────────────────────────────────────┐
│     业务组件 (Business Components)      │
│  绑定具体业务逻辑，如 KnowledgeCard     │
├────────────────────────────────────────┤
│     通用业务组件 (Common Components)    │
│  可复用的业务组件，如 PageHeader        │
├────────────────────────────────────────┤
│     基础组件 (Base Components)          │
│  纯UI组件，如按钮、表单、表格等          │
└────────────────────────────────────────┘
```

#### 3.2.2 组件命名规范

| 类型 | 规范 | 示例 |
|-----|------|------|
| 页面组件 | View结尾 | KnowledgeDetailView |
| 布局组件 | Layout结尾 | DefaultLayout |
| 业务组件 | 模块前缀+功能名 | PlanProgressRing |
| 通用组件 | 功能名 | Pagination |

---

## 4. 后端架构

### 4.1 分层设计

```
┌─────────────────────────────────────────────────────┐
│                  Presentation 层 (接口层)            │
│   Controller: 处理请求、参数校验、调用Service        │
├─────────────────────────────────────────────────────┤
│                   Service 层 (业务层)               │
│   业务逻辑处理、事务管理、复杂计算                   │
├─────────────────────────────────────────────────────┤
│                  Repository 层 (数据层)              │
│   数据访问、缓存操作、ORM映射                       │
├─────────────────────────────────────────────────────┤
│                    Entity 层 (实体层)                │
│   数据库表映射对象                                  │
└─────────────────────────────────────────────────────┘
```

### 4.2 模块划分

```
studyhelper-backend/
├── studyhelper-common/              # 公共模块
│   ├── src/main/java/
│   │   └── com/studyhelper/common/
│   │       ├── config/             # 公共配置
│   │       │   ├── RedisConfig.java
│   │       │   └── WebConfig.java
│   │       │
│   │       ├── constant/          # 常量定义
│   │       │   ├── UserConstant.java
│   │       │   ├── RedisKey.java
│   │       │   └── BusinessType.java
│   │       │
│   │       ├── exception/         # 异常处理
│   │       │   ├── GlobalExceptionHandler.java
│   │       │   ├── BusinessException.java
│   │       │   └── ErrorCode.java
│   │       │
│   │       ├── result/            # 统一响应
│   │       │   ├── Result.java
│   │       │   ├── ResultCode.java
│   │       │   └── PageResult.java
│   │       │
│   │       ├── util/              # 工具类
│   │       │   ├── JwtUtil.java
│   │       │   ├── IpUtil.java
│   │       │   └── PasswordUtil.java
│   │       │
│   │       └── annotation/       # 自定义注解
│   │           ├── LoginRequired.java
│   │           ├── Permission.java
│   │           └── Log.java
│   │
│   └── src/main/resources/
│       └── mapper/                # Mapper XML
│
├── studyhelper-user/               # 用户模块
│   └── src/main/java/com/studyhelper/user/
│       ├── controller/
│       │   └── UserController.java
│       ├── service/
│       │   ├── UserService.java
│       │   └── impl/UserServiceImpl.java
│       ├── repository/
│       │   └── UserMapper.java
│       ├── entity/
│       │   └── User.java
│       └── dto/
│           ├── LoginRequest.java
│           ├── RegisterRequest.java
│           └── UserProfileVO.java
│
├── studyhelper-knowledge/          # 知识库模块
│   └── src/main/java/com/studyhelper/knowledge/
│       ├── controller/
│       │   ├── SubjectController.java
│       │   ├── ChapterController.java
│       │   └── KnowledgeController.java
│       ├── service/
│       │   ├── SubjectService.java
│       │   ├── ChapterService.java
│       │   ├── KnowledgeService.java
│       │   └── impl/
│       ├── repository/
│       │   ├── SubjectMapper.java
│       │   ├── ChapterMapper.java
│       │   └── KnowledgeMapper.java
│       ├── entity/
│       │   ├── Subject.java
│       │   ├── Chapter.java
│       │   └── Knowledge.java
│       └── dto/
│
├── studyhelper-plan/               # 学习计划模块
│   └── src/main/java/com/studyhelper/plan/
│       ├── controller/
│       │   ├── PlanController.java
│       │   └── TaskController.java
│       ├── service/
│       │   ├── PlanService.java
│       │   ├── TaskService.java
│       │   └── impl/
│       ├── repository/
│       ├── entity/
│       │   ├── StudyPlan.java
│       │   └── DailyTask.java
│       └── dto/
│
├── studyhelper-exam/               # 考试模块
│   └── src/main/java/com/studyhelper/exam/
│       ├── controller/
│       │   ├── QuestionController.java
│       │   ├── PaperController.java
│       │   └── ExamController.java
│       ├── service/
│       │   ├── QuestionService.java
│       │   ├── PaperService.java
│       │   ├── ExamService.java
│       │   ├── PaperGenerateService.java  # 智能组卷
│       │   └── impl/
│       ├── repository/
│       ├── entity/
│       │   ├── Question.java
│       │   ├── Paper.java
│       │   └── ExamRecord.java
│       └── dto/
│
├── studyhelper-generator/          # 代码生成模块(可选)
│
└── studyhelper-admin/              # 管理后台模块(可选)
```

### 4.3 数据库设计

#### 4.3.1 ER图概念模型

```
┌─────────┐     ┌─────────┐     ┌─────────┐
│  Users  │────<│StudyRec │     │ Subjects│
│  用户表  │     │学习记录 │     │ 学科表  │
└────┬────┘     └─────────┘     └────┬────┘
     │                               │
     │     ┌─────────┐     ┌─────────┴────┐
     │     │ StudyPlan│     │  Chapters   │
     │     │ 学习计划 │────<│  章节表     │
     │     └────┬────┘     └────┬────────┘
     │          │              │
     │    ┌─────┴─────┐        │
     │    │ DailyTask │   ┌────┴────────┐
     │    │ 每日任务  │   │ Knowledge   │
     │    └───────────┘   │ 知识点表    │
     │                    └─────────────┘
     │
┌────┴────────┐     ┌─────────┐     ┌─────────┐
│  ExamRecord │────<│  Paper  │────<│ Question│
│  考试记录   │     │ 试卷表  │     │ 题目表  │
└─────────────┘     └─────────┘     └─────────┘
```

#### 4.3.2 核心表结构

```sql
-- 用户表
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    role ENUM('student', 'parent', 'admin') NOT NULL,
    real_name VARCHAR(50),
    phone VARCHAR(20),
    email VARCHAR(100),
    grade TINYINT COMMENT '1=高一,2=高二,3=高三',
    avatar_url VARCHAR(255),
    bind_code VARCHAR(6) COMMENT '家长绑定码',
    parent_user_id INT COMMENT '关联家长ID',
    study_target VARCHAR(50) COMMENT '升学目标',
    weak_subjects VARCHAR(200) COMMENT '薄弱学科(JSON)',
    status TINYINT DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_role (role),
    INDEX idx_grade (grade)
);

-- 学科表
CREATE TABLE subjects (
    id INT PRIMARY KEY AUTO_INCREMENT,
    subject_name VARCHAR(50) NOT NULL,
    subject_code VARCHAR(20) NOT NULL,
    grade_level TINYINT DEFAULT 4 COMMENT '1=高一,2=高二,3=高三,4=通用',
    icon_url VARCHAR(255),
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 章节表
CREATE TABLE chapters (
    id INT PRIMARY KEY AUTO_INCREMENT,
    subject_id INT NOT NULL,
    chapter_name VARCHAR(100) NOT NULL,
    chapter_code VARCHAR(50),
    parent_id INT DEFAULT 0 COMMENT '父章节ID',
    level TINYINT DEFAULT 1 COMMENT '层级',
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (subject_id) REFERENCES subjects(id),
    INDEX idx_subject (subject_id)
);

-- 知识点表
CREATE TABLE knowledge_points (
    id INT PRIMARY KEY AUTO_INCREMENT,
    chapter_id INT NOT NULL,
    title VARCHAR(200) NOT NULL,
    content TEXT COMMENT '知识点内容(富文本)',
    importance_level TINYINT DEFAULT 3 COMMENT '重要程度1-5',
    difficulty_level TINYINT DEFAULT 3 COMMENT '难度1-5',
    exam_frequency VARCHAR(20) DEFAULT '中频' COMMENT '高频/中频/低频',
    tags VARCHAR(500) COMMENT '标签JSON',
    related_ids VARCHAR(500) COMMENT '关联知识点ID',
    view_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (chapter_id) REFERENCES chapters(id),
    INDEX idx_chapter (chapter_id),
    INDEX idx_title (title),
    FULLTEXT idx_content (title, content)
);

-- 学习计划表
CREATE TABLE study_plans (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    plan_name VARCHAR(100) NOT NULL,
    plan_type TINYINT DEFAULT 1 COMMENT '1=日常,2=冲刺,3=专项',
    target_date DATE,
    total_days INT DEFAULT 0,
    completed_days INT DEFAULT 0,
    status TINYINT DEFAULT 0 COMMENT '0=草稿,1=进行中,2=已完成,3=已放弃',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    INDEX idx_user (user_id),
    INDEX idx_status (status)
);

-- 每日任务表
CREATE TABLE daily_tasks (
    id INT PRIMARY KEY AUTO_INCREMENT,
    plan_id INT NOT NULL,
    task_date DATE NOT NULL,
    subject_id INT,
    chapter_id INT,
    knowledge_ids VARCHAR(500) COMMENT '知识点ID列表JSON',
    target_count INT DEFAULT 0,
    completed_count INT DEFAULT 0,
    estimated_minutes INT DEFAULT 30,
    actual_minutes INT DEFAULT 0,
    status TINYINT DEFAULT 0 COMMENT '0=待完成,1=进行中,2=已完成',
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (plan_id) REFERENCES study_plans(id),
    INDEX idx_plan (plan_id),
    INDEX idx_date (task_date)
);

-- 题目表
CREATE TABLE questions (
    id INT PRIMARY KEY AUTO_INCREMENT,
    subject_id INT NOT NULL,
    chapter_id INT,
    question_type TINYINT NOT NULL COMMENT '1=单选,2=多选,3=填空,4=简答,5=计算,6=作文',
    difficulty TINYINT DEFAULT 2 COMMENT '1=简单,2=中等,3=困难',
    score INT DEFAULT 5,
    content TEXT NOT NULL COMMENT '题干',
    options TEXT COMMENT '选项JSON(选择题)',
    answer TEXT NOT NULL COMMENT '答案',
    analysis TEXT COMMENT '解析',
    knowledge_ids VARCHAR(500) COMMENT '考查知识点JSON',
    usage_count INT DEFAULT 0,
    correct_rate DECIMAL(5,2) DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (subject_id) REFERENCES subjects(id),
    INDEX idx_subject (subject_id),
    INDEX idx_chapter (chapter_id),
    INDEX idx_type (question_type),
    INDEX idx_difficulty (difficulty)
);

-- 试卷表
CREATE TABLE papers (
    id INT PRIMARY KEY AUTO_INCREMENT,
    paper_name VARCHAR(100) NOT NULL,
    subject_id INT NOT NULL,
    grade_level TINYINT,
    total_score INT DEFAULT 100,
    total_minutes INT DEFAULT 120,
    question_count INT DEFAULT 0,
    difficulty_avg DECIMAL(3,2),
    source_type TINYINT DEFAULT 1 COMMENT '1=手动,2=智能',
    generate_params TEXT COMMENT '生成参数JSON',
    created_by INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (subject_id) REFERENCES subjects(id),
    INDEX idx_subject (subject_id)
);

-- 试卷题目关联表
CREATE TABLE paper_questions (
    id INT PRIMARY KEY AUTO_INCREMENT,
    paper_id INT NOT NULL,
    question_id INT NOT NULL,
    sort_order INT DEFAULT 0,
    score INT,
    FOREIGN KEY (paper_id) REFERENCES papers(id),
    FOREIGN KEY (question_id) REFERENCES questions(id),
    UNIQUE KEY uk_paper_question (paper_id, question_id)
);

-- 考试记录表
CREATE TABLE exam_records (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    paper_id INT NOT NULL,
    total_score INT,
    obtained_score INT,
    duration_minutes INT,
    answers_json TEXT COMMENT '答题记录JSON',
    wrong_question_ids VARCHAR(500) COMMENT '错题ID列表',
    status TINYINT DEFAULT 0 COMMENT '0=进行中,1=已完成',
    started_at TIMESTAMP,
    submitted_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (paper_id) REFERENCES papers(id),
    INDEX idx_user (user_id),
    INDEX idx_paper (paper_id)
);

-- 学习记录表
CREATE TABLE study_records (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    knowledge_id INT NOT NULL,
    study_duration INT DEFAULT 0 COMMENT '学习时长(秒)',
    status TINYINT DEFAULT 0 COMMENT '0=未完成,1=已掌握',
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (knowledge_id) REFERENCES knowledge_points(id),
    UNIQUE KEY uk_user_knowledge (user_id, knowledge_id)
);

-- 收藏表
CREATE TABLE favorites (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    target_type VARCHAR(20) NOT NULL COMMENT 'knowledge/question/paper',
    target_id INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    UNIQUE KEY uk_user_target (user_id, target_type, target_id)
);

-- 操作日志表
CREATE TABLE operation_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50),
    operation VARCHAR(100),
    method VARCHAR(200),
    params TEXT,
    result TEXT,
    ip VARCHAR(50),
    user_agent TEXT,
    duration INT COMMENT '耗时(毫秒)',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_created (created_at)
);
```

---

## 5. 部署架构

### 5.1 开发环境

```
┌─────────────────────────────────────────────────────────────┐
│                      开发环境 (Dev)                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  前端开发者                                                 │
│     │                                                       │
│     ▼                                                       │
│  ┌──────────────────┐    ┌──────────────────┐              │
│  │   Vite Dev       │    │   MySQL Dev      │              │
│  │   Server         │    │   localhost:3306 │              │
│  │   localhost:3000 │    └──────────────────┘              │
│  └────────┬─────────┘                                      │
│           │                                                  │
│           ▼                                                  │
│  ┌──────────────────┐    ┌──────────────────┐              │
│  │   Spring Boot    │    │   Redis Dev       │              │
│  │   Dev Server     │    │   localhost:6379  │              │
│  │   localhost:8080 │    └──────────────────┘              │
│  └──────────────────┘                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 测试环境

```
┌─────────────────────────────────────────────────────────────┐
│                      测试环境 (Test)                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                    Docker Compose                      │ │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐      │ │
│  │  │   Nginx    │  │  Frontend  │  │  Backend   │      │ │
│  │  │  (反向代理) │  │  (前端服务) │  │  (后端服务) │      │ │
│  │  └────────────┘  └────────────┘  └────────────┘      │ │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐      │ │
│  │  │   MySQL    │  │   Redis    │  │   MinIO    │      │ │
│  │  │  (测试库)  │  │  (缓存)    │  │  (文件存储) │      │ │
│  │  └────────────┘  └────────────┘  └────────────┘      │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                             │
│  域名: test-api.studyhelper.com                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 生产环境

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           生产环境 (Production)                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│                           ┌─────────────┐                               │
│                           │   域名解析   │                               │
│                           │  DNS/CDN    │                               │
│                           └──────┬──────┘                               │
│                                  │                                       │
│                                  ▼                                       │
│                           ┌─────────────┐                               │
│                           │   Nginx     │                               │
│                           │  负载均衡    │                               │
│                           │ SSL终结     │                               │
│                           └──────┬──────┘                               │
│                    ┌─────────────┼─────────────┐                         │
│                    ▼             ▼             ▼                         │
│              ┌──────────┐  ┌──────────┐  ┌──────────┐                    │
│              │ Backend  │  │ Backend  │  │ Backend  │                    │
│              │ Node 1   │  │ Node 2   │  │ Node N   │                    │
│              │ :8080    │  │ :8080    │  │ :8080    │                    │
│              └────┬─────┘  └────┬─────┘  └────┬─────┘                    │
│                   │            │            │                            │
│         ┌─────────┴────────────┴────────────┴─────────┐                  │
│         │                                              │                  │
│         ▼                                              ▼                  │
│  ┌──────────────┐                           ┌──────────────┐             │
│  │    MySQL     │                           │    Redis     │             │
│  │   主从集群   │                           │   集群       │             │
│  │ Master-Slave│                           │  (哨兵模式)  │             │
│  └──────────────┘                           └──────────────┘             │
│         │                                              │                  │
│         └──────────────────┬───────────────────────────┘                  │
│                            ▼                                              │
│                     ┌──────────────┐                                     │
│                     │     OSS      │                                     │
│                     │  (文件存储)   │                                     │
│                     └──────────────┘                                     │
│                                                                         │
│  域名: api.studyhelper.com (后端)                                        │
│        www.studyhelper.com (前端)                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.4 部署配置

#### Docker Compose 配置 (生产简化版)

```yaml
version: '3.8'
services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - frontend
      - backend
    networks:
      - studyhelper-net

  frontend:
    image: studyhelper-frontend:latest
    environment:
      - VITE_API_BASE_URL=https://api.studyhelper.com
    networks:
      - studyhelper-net

  backend:
    image: studyhelper-backend:latest
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/studyhelper
      - SPRING_REDIS_HOST=redis
    depends_on:
      - mysql
      - redis
    networks:
      - studyhelper-net

  mysql:
    image: mysql:8.0
    volumes:
      - mysql-data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=studyhelper
    networks:
      - studyhelper-net

  redis:
    image: redis:7-alpine
    volumes:
      - redis-data:/data
    networks:
      - studyhelper-net

networks:
  studyhelper-net:
    driver: bridge

volumes:
  mysql-data:
  redis-data:
```

---

## 6. 安全架构

### 6.1 认证与授权

```
┌─────────────────────────────────────────────────────────────────┐
│                        认证授权流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  用户请求 ──► 过滤器链 ──► JWT验证 ──► 权限校验 ──► 业务处理      │
│                    │           │           │                    │
│                    ▼           ▼           ▼                    │
│              ┌──────────┐ ┌────────┐ ┌────────────┐           │
│              │ XSS过滤  │ │Token   │ │ 角色权限   │           │
│              │ SQL过滤  │ │解析    │ │ 验证       │           │
│              └──────────┘ └────────┘ └────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.1.1 JWT Token 结构

```json
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "userId": 12345,
    "username": "student001",
    "role": "student",
    "grade": 2,
    "exp": 1715600000,
    "iat": 1715513600
  },
  "signature": "..."
}
```

#### 6.1.2 权限矩阵

| 功能 | 学生 | 家长 | 管理员 |
|-----|-----|-----|-------|
| 查看知识库 | ✓ | ✗ | ✓ |
| 管理知识库 | ✗ | ✗ | ✓ |
| 学习知识点 | ✓ | ✗ | ✓ |
| 创建学习计划 | ✓ | ✗ | ✓ |
| 查看计划(本人) | ✓ | ✓(子女) | ✓ |
| 生成试卷 | ✓ | ✗ | ✓ |
| 参加考试 | ✓ | ✗ | ✗ |
| 查看成绩 | ✓ | ✓(子女) | ✓ |
| 用户管理 | ✗ | ✗ | ✓ |

### 6.2 数据安全

#### 6.2.1 密码安全

- 存储: BCrypt 加密 (强度因子 12)
- 传输: HTTPS 全站加密
- 校验: 前端加盐后传输

#### 6.2.2 敏感数据处理

| 数据类型 | 处理方式 |
|---------|---------|
| 用户密码 | BCrypt加密，不返回前端 |
| 手机号 | 部分脱敏 (138****5678) |
| 真实姓名 | 可配置返回 |
| 身份证 | 不存储 |
| 考试答案 | 加密存储 |

#### 6.2.3 SQL注入防护

- 使用 MyBatis-Plus 的参数化查询
- 禁止字符串拼接SQL
- 前端输入长度限制

#### 6.2.4 XSS防护

- 富文本内容过滤 (白名单标签)
- JSON响应特殊字符转义
- HttpOnly Cookie

### 6.3 安全配置

```java
// Spring Security 配置
@Configuration
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())  // 前后端分离，JWT认证
            .cors(cors -> cors.configurationSource(corsConfigurationSource()))
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**", "/api/public/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthenticationFilter(), 
                UsernamePasswordAuthenticationFilter.class);
        
        return http.build();
    }
}
```

### 6.4 日志审计

```java
// 操作日志注解
@Log(title = "用户登录", businessType = BusinessType.LOGIN)
@PostMapping("/login")
public Result<LoginVO> login(@RequestBody @Valid LoginRequest request) {
    // 登录逻辑
}

// 日志切面
@Aspect
@Component
public class LogAspect {
    
    @Around("@annotation(log)")
    public Object around(ProceedingJoinPoint joinPoint, Log log) {
        // 记录操作日志
    }
}
```

---

## 7. 性能优化策略

### 7.1 前端优化

| 优化项 | 策略 |
|-------|------|
| 首屏加载 | 代码分割、懒加载 |
| 图片 | OSS压缩、WebP格式 |
| 请求 | 请求合并、缓存 |
| 代码 | Tree Shaking、DCE |

### 7.2 后端优化

| 优化项 | 策略 |
|-------|------|
| 数据库 | 索引优化、SQL优化 |
| 缓存 | Redis多级缓存 |
| 连接池 | Druid连接池 |
| 异步 | 消息队列解耦 |

### 7.3 缓存策略

```
┌─────────────────────────────────────────────────────────┐
│                    缓存层级设计                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  L1: 浏览器缓存  (静态资源, 1天)                         │
│       │                                                  │
│       ▼                                                  │
│  L2: CDN缓存     (静态资源, 7天)                         │
│       │                                                  │
│       ▼                                                  │
│  L3: Nginx缓存   (接口响应, 1小时)                       │
│       │                                                  │
│       ▼                                                  │
│  L4: Redis缓存   (热点数据, 30分钟)                      │
│       │                                                  │
│       ▼                                                  │
│  L5: 数据库      (持久化数据)                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 7.4 Redis 缓存Key设计

| 缓存对象 | Key格式 | TTL | 说明 |
|---------|--------|-----|------|
| 用户Session | `session:user:{userId}` | 24h | 用户会话信息 |
| 学科列表 | `subject:list` | 1h | 学科缓存 |
| 章节树 | `chapter:tree:{subjectId}` | 1h | 章节结构 |
| 知识点详情 | `knowledge:{id}` | 30min | 知识点内容 |
| 热门题目 | `question:hot:{subjectId}` | 1h | 热门题目 |
| 用户Token | `token:{token}` | 24h | Token黑名单/验证 |

---

## 8. 监控与运维

### 8.1 监控指标

| 类型 | 指标 | 告警阈值 |
|-----|------|---------|
| 可用性 | 服务可用率 | < 99.5% |
| 性能 | API响应时间P99 | > 500ms |
| 性能 | 页面加载时间 | > 3s |
| 容量 | CPU使用率 | > 80% |
| 容量 | 内存使用率 | > 85% |
| 容量 | 磁盘使用率 | > 90% |
| 业务 | 日活用户 | 异常下降 |

### 8.2 日志规范

```xml
<!-- Logback 配置 -->
<encoder>
    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
</encoder>

<!-- 日志级别 -->
ERROR: 系统异常、业务异常
WARN: 参数校验失败、性能警告
INFO: 用户操作、系统事件
DEBUG: 开发调试日志 (生产关闭)
```

---

## 9. 附录

### 9.1 环境变量配置

```bash
# 前端 .env.production
VITE_API_BASE_URL=https://api.studyhelper.com
VITE_APP_TITLE=StudyHelper

# 后端 application-prod.yml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/studyhelper?useSSL=true
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  redis:
    host: ${REDIS_HOST}
    password: ${REDIS_PASSWORD}
  servlet:
    multipart:
      oss:
        endpoint: ${OSS_ENDPOINT}
        access-key: ${OSS_AK}
        secret-key: ${OSS_SK}
        bucket: studyhelper
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000
```

### 9.2 文档版本

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| V1.0 | 2026-05-11 | 初始架构设计 | Hermes Agent |
